# my-hub

Mobile PWA launcher. It reads `projects.json` and renders project cards.

## Add a project

Edit `projects.json` and push to `main`. The launcher fetches the latest file when network is available and falls back to the cached copy offline.

## Install

- iPhone: Safari -> Share -> Add to Home Screen
- Android: Chrome -> menu -> Install app / Add to Home Screen
